<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{ifset title}{include title|stripHtml} | {/ifset}EShop Admin</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --purple-main: #523091;
            --purple-dark: #271744;
            --glass-bg: rgba(255, 255, 255, 0.75);
            --glass-border: rgba(255, 255, 255, 0.3);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #250e3c 0%, #440a7d 100%);
            min-height: 100vh;
            color: #333;
            margin: 0;
        }

        /* UI Components Styling */
        .glass-card {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            border-radius: 15px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
        }

        .navbar-glass {
            background: var(--glass-border);
            backdrop-filter: blur(5px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .btn-purple {
            background-color: var(--purple-main);
            color: white;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .btn-purple:hover {
            background-color: var(--purple-dark);
            color: white;
            transform: translateY(-2px);
        }

        /* Toast Notifications */
        .flash-container {
            position: fixed;
            top: 90px;
            right: 20px;
            z-index: 9999;
            pointer-events: none;
        }

        .flash {
            pointer-events: auto;
            min-width: 280px;
            padding: 12px 25px !important;
            margin-bottom: 10px !important;
            border-radius: 50px !important;
            font-size: 0.9rem;
            font-weight: 500;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2) !important;
            border: 1px solid rgba(255,255,255,0.2) !important;
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            animation: slideIn 0.3s ease-out;
        }

        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        .form-control { border-radius: 10px; padding: 0.6rem 1rem; }
        .table { background: transparent !important; }
        .table thead th {
            background: rgba(82, 48, 145, 0.1);
            color: var(--purple-dark);
            border-bottom: 2px solid var(--purple-main);
        }
        .text-purple { color: var(--purple-main); }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-glass sticky-top mb-4">
        <div class="container">
            <a class="navbar-brand fw-bold" n:href="Home:default">EShop</a>
            
            <div class="d-flex align-items-center">
                <span n:if="$user->isLoggedIn()" class="text-white me-3 d-none d-md-inline">
                    <small>Uživatel:</small> <strong>{$user->getIdentity()->email}</strong>
                </span>
                <a n:if="$user->isLoggedIn()" n:href="Sign:out" class="btn btn-sm btn-outline-light">Odhlásit se</a>
            </div>
        </div>
    </nav>

    <div class="flash-container">
        <div n:foreach="$flashes as $flash" 
             class="flash glass-card border-0 alert alert-{$flash->type === 'error' ? 'danger' : 'success'} shadow">
             <span class="me-2">{if $flash->type === 'error'}{/if}</span>
             {$flash->message}
        </div>
    </div>

    <div class="container pb-5">
        <main class="glass-card p-4 p-md-5">
            {include content}
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Auto-dismiss flash messages after 4 seconds
        setTimeout(function() {
            let flashes = document.querySelectorAll('.flash');
            flashes.forEach(el => {
                el.style.transition = "opacity 0.6s ease, transform 0.6s ease";
                el.style.opacity = "0";
                el.style.transform = "translateX(20px)";
                setTimeout(() => el.remove(), 600);
            });
        }, 4000);
    </script>
</body>
</html>